import { Text, View } from 'react-native';
export default function AideScreen() {
  return (
    <View style={{flex:1, alignItems:'center', justifyContent:'center'}}>
      <Text>Aide — En construction</Text>
    </View>
  );
}