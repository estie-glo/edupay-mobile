import { Text, View } from 'react-native';
export default function ReclamationsScreen() {
  return (
    <View style={{flex:1, alignItems:'center', justifyContent:'center'}}>
      <Text>Reclamations Parent — En construction</Text>
    </View>
  );
}